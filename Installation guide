# 🛠️ Installation Guide

Follow these steps to set up and run the selfbot locally.

---

### 🔰 Requirements

* Python 3.8 or higher
* Git
* A terminal (Command Prompt, PowerShell, or Linux/macOS Terminal)
* Discord user token (⚠️ for educational use only)

---

### 📦 Step-by-Step Setup

#### 1. Clone the repository

```bash
git clone https://github.com/yourusername/selfbot
cd selfbot
```

#### 2. Install Python dependencies

Make sure you have `pip` installed, then run:

```bash
pip install -r requirements.txt
```

If you don’t have a `requirements.txt`, create one with:

```txt
discord.py
colorama
python-dotenv
```

#### 3. Create a `.env` file

In the same folder as `main.py`, create a `.env` file with the following content:

```env
DISCORD_TOKEN=your_discord_user_token_here
```

> ⚠️ **Never share your token publicly!** Do not commit `.env` to GitHub.

#### 4. Run the bot

```bash
python main.py
```

If everything is set up correctly, you'll see:

```
✅ Logged in as: YourDiscordUsername
⚡ Selfbot is now active and listening...
```

---

### ✅ Done!

You can now use commands like `.spam`, `.ping`, `.typewrite`, etc. directly from any server where your account is a member.

---

## ⚠️ Warning

* This project is a **selfbot** and violates Discord’s [Terms of Service](https://discord.com/terms).
* It is intended **only for testing and learning**.
* You assume all responsibility if you use this bot on your account.
